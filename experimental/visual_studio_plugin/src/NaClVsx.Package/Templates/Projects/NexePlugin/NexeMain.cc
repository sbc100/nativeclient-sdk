//
// TODO(mmortensen):  The code below is wrong (nexe should not have a main),
//        BUT the current toolchain doesn't work with Pepper2 yet, and
//        I want to populate this with something like hello_world or the
//        stub starting point -- I will do that soon!
#include <stdio.h>

int main(int argc, char* argv[]) {
  return 0;
}
